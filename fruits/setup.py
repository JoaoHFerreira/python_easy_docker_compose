from setuptools import setup, find_packages

setup(
    name='fruits',  # The name of your package
    version='0.1',   # The version of your package
    packages=find_packages(),  # Automatically find packages within your project
)